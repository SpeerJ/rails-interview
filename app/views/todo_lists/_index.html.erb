<%= turbo_frame_tag 'todo_lists' do %>
  <% todo_lists.each do |todo_list| %>
    <div>
      #<%= todo_list.id %> | <%= todo_list.name %>
      | <%= link_to "View Todo Items", todo_list_todo_items_path(todo_list), data: { turbo: "true", turbo_frame: "_top" }  %>
    </div>
  <% end %>
<% end %>